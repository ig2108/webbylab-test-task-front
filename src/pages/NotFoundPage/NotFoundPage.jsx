import React from 'react';
// import PropTypes from 'prop-types';
import styles from './NotFoundPage.module.css';

const NotFoundPage = () => {
  return <h1 className={styles.notFoundTitle}>Page Not Found</h1>;
};

export default NotFoundPage;
